# DataForge 全流程项目计划

> 版本：v0.7
> 更新日期：2026-08-14
> 当前重点：内部单机部署稳定化与完整流程联调

## 1. 项目目标

DataForge 以 OpenDCAI DataFlow 为核心数据处理引擎，把 PDF、CSV、Excel（XLSX）、Markdown、Word（DOCX）、TXT、JSON 和 JSONL 等源文档，稳定地加工为格式固定、可以入库、可以逐条追溯的知识资产，再通过独立的索引过程将知识资产发布为可检索数据。

核心原则是：

> 业务用户只确定“处理哪些文档、生成什么内容”；系统自动匹配后台已发布的默认标准流程。

平台统一管理来源、版本、知识格式、标准流程、加工任务、校验结果、知识库、索引版本和溯源关系；DataFlow 继续负责算子、流程编排、模型服务和实际执行。

登录、用户、角色和租户权限暂不进入一期范围。

### 1.1 产品与交互原则

- 业务工作区面向不懂技术的使用者，只展示文档、处理任务、知识库和业务结果。
- 流程开发区面向技术人员，承载知识类型、流程调试、样本验证、模型算力和标准流程发布。
- 业务页面不展示 Pipeline、算子、Prompt、JSON Schema、模型参数和内部任务编号。
- 每个页面只设置一个主要操作；复杂配置渐进展开，不与日常使用混排。
- 每个可点击按钮必须对应真实可执行的操作，未实现的能力不以按钮形式出现。
- 跨页面操作必须保留来源页面和状态；多步骤流程必须提供上一步、取消和返回入口。
- 操作完成后必须落在可继续使用的结果页，例如上传后回到文档详情、创建任务后回到任务详情、调试后回到标准流程发布。

## 2. 用户主流程

```text
上传源文档
  → 保存文件来源和不可变版本
  → 选择目标知识库类型
  → 系统匹配该类型已发布的默认标准流程
  → 用户确认知识库名称并启动任务
  → 多份文档并行处理
  → 按目标知识格式逐条验证
  → 验证通过后写入知识库
  → 从任一知识记录追溯到源文档版本和原始位置
```

如果没有兼容且已验证的默认标准流程，用户不能启动正式加工；页面给出明确说明，技术人员可进入“标准流程 / DataFlow 调试台”完成流程定义、样本运行和输出格式验证。

### 2.1 入库与索引是两个独立阶段

标准知识记录写入关系数据库，只表示“知识资产已入库”，不表示已经完成向量化或可以被应用检索。后续必须执行独立、异步、可重试的索引任务：

```text
知识资产已入库
  → 匹配该知识类型已发布的默认索引方案
  → 创建异步索引任务
  → 分批生成向量或图索引
  → 写入向量库或图数据库
  → 校验记录数、维度和映射关系
  → 生成可用的索引版本
```

如果尚未配置默认索引方案，知识库保持“已入库，待索引”，不将其误报为加工失败。业务用户只看到“待索引、索引中、可检索、索引失败”等结果状态；模型、数据库、批大小和并发等技术配置只在流程开发区维护。

## 3. 一期业务模块

### 3.1 源文档中心

优先输入：

- PDF：读取已有文字层，扫描件 OCR 后续扩展
- CSV：每一行作为一条源记录
- Markdown、TXT：读取正文
- Word：一期支持 DOCX

管理对象：

- `source`：逻辑上的文件来源
- `source_version`：一次不可变的文件内容版本
- 原文件、文件名、类型、大小、校验码、创建时间

相同来源上传新内容时生成新版本；相同内容重复上传时复用已有版本。

### 3.2 知识库类型目录

知识库类型由平台提前定义，每种类型都有固定输出结构。第一批类型：

| 类型 | 每条知识记录的必需字段 | 主要用途 |
|---|---|---|
| 文本块知识库 | `content`、`chunk_index` | 文档检索、RAG |
| 问答知识库 | `question`、`answer` | FAQ、标准问答 |
| 知识图谱三元组 | `subject`、`predicate`、`object` | 图谱查询、实体关系 |
| 多轮对话知识库 | `messages` | 对话训练、评测 |

知识库类型不是用户临时创建的自由表单，而是平台治理对象。变更字段时必须形成新版本，并重新验证关联流程。

### 3.3 标准流程目录

标准流程不是在生产任务中临时编排，而是在独立区域提前完成：

1. 在 DataFlow 中选择算子并编排流程。
2. 绑定唯一目标知识库类型和输出结构版本。
3. 使用代表性样本运行。
4. 对全部输出记录执行格式验证。
5. 验证通过后发布为“可用于正式加工”的标准流程版本。

标准流程状态：

- `configured`：已配置，尚未验证，不可正式使用
- `validated`：样本和输出格式验证通过，可正式使用
- `inactive`：已停用，历史任务仍保留引用

生产页不让业务人员选择标准流程，由系统执行以下兼容规则；前端只展示可用知识类型，后端负责最终写入保护：

```text
流程.目标知识类型 = 用户选择的知识类型
AND 流程.输出结构版本 = 类型.结构版本
AND 流程.验证状态 = 已验证
AND 流程.状态 = 启用
```

前端过滤用于减少误选，后端校验是最终写入保护，不能只依赖页面逻辑。

### 3.4 知识生产任务

任务输入：

- 知识库名称
- 一个目标知识库类型
- 一个兼容的标准流程版本
- 一个或多个源文档版本

执行逻辑：

- 不同源文档可以并行执行，当前单任务并发上限为 4
- 单份文档内部由 DataFlow 按流程顺序执行算子
- 汇总结果后逐条做固定结构验证
- 任一记录不符合结构时，本次知识库不入库，任务保留失败明细
- 全部通过后一次性创建知识库及其知识记录

任务需要保留状态、进度、错误、校验数量和目标知识库编号。

### 3.5 知识资产中心

一期入库后的核心对象：

- `knowledge_base`：一个独立的知识库资产
- `knowledge_record`：知识库中的一条标准知识记录

知识资产以关系数据库中的标准记录为事实来源。知识库状态为“已入库”时，代表结构校验和关系库写入已经完成；向量库和图数据库是可重新构建的派生索引，不能代替原始知识记录。

知识库至少记录：

- 知识库编号、名称和知识类型
- 使用的标准流程版本
- 生产任务编号
- 记录数量、状态和入库时间

每条知识记录至少记录：

- 所属知识库和记录序号
- 标准化后的最终内容
- 源文档版本编号
- DataFlow 执行任务和中间资产版本
- 源位置，例如页码、CSV 行号、原始记录序号、分块序号

### 3.6 数据溯源

一期必须支持从一条知识记录反向查询：

```text
知识记录
  → 所属知识库
  → 标准流程版本与生产任务
  → DataFlow 运行任务
  → 源文档版本
  → 原始文件和源位置
```

对于 PDF，后续解析器应补充页码和字符范围；对于 CSV 应补充行号；对于 Markdown、Word 和 TXT 应逐步补充段落或字符范围。当前无法精确定位时，至少保留源记录序号和分块序号。

### 3.7 流程开发区与 DataFlow 调试台

DataForge 不复制或向业务用户暴露 DataFlow 的完整产品功能。流程开发区保留技术人员真正需要的能力：

- 知识类型和输出结构配置
- 算子目录与参数配置
- 可视化流程编排
- 样本数据、任务执行、日志和中间结果
- 输出格式和质量验证
- 模型与算力配置
- 标准流程发布、默认版本和停用治理
- 向量模型服务、向量库和图数据库连接配置
- 不同知识类型的索引方案、样本验证、发布和默认版本治理

当前可以嵌入完整 DataFlow WebUI 作为过渡实现；后续逐步收敛为上述必要界面。DataForge 负责来源、任务、标准流程、知识资产和溯源治理；DataFlow 只负责流程的定义、调试和执行。

## 4. 一期数据关系

```text
source 1 ── N source_version

knowledge_type 1 ── N standard_pipeline

knowledge_job N ── N source_version
knowledge_job N ── 1 standard_pipeline
knowledge_job 1 ── 0..1 knowledge_base

knowledge_base 1 ── N knowledge_record
knowledge_record N ── 1 source_version
knowledge_record N ── 1 run
```

关键约束：一个知识库只有一种知识类型；一个标准流程版本只绑定一种知识类型；知识记录不能脱离源版本独立存在。

## 5. 二期：知识索引与检索准备

二期将已经写入关系数据库的知识记录转换为应用可检索的索引。索引是知识资产的派生版本，必须能够重建、切换和追溯，不能在知识记录上直接覆盖更新。

### 5.1 后台配置对象

技术人员在流程开发区维护以下对象，业务用户不接触这些参数：

- `embedding_service`：向量模型名称、服务地址、维度、批大小、并发、超时、重试和密钥引用
- `reranker_service`：可选重排模型名称、兼容接口、超时、重试和密钥引用；由检索方案决定是否启用
- `vector_store`：向量数据库类型、连接地址、命名规则、认证引用和连接测试
- `graph_store`：图数据库连接、图空间和认证引用；仅知识图谱类型需要
- `index_profile`：知识类型与结构版本、索引字段、模型、目标存储、元数据规则、检索参数和版本
- `knowledge_index`：某个知识库基于某个索引方案产生的不可变索引版本
- `index_job`：一次异步索引或重建任务
- `index_batch`：任务中的一个可重试批次和检查点
- `index_record`：知识记录到向量或图索引记录的映射

索引方案与标准流程一样，需要经过“配置、样本验证、发布、设为默认、停用”的治理过程。一个知识类型和结构版本可以存在多个索引方案，但生产自动索引只能匹配一个已发布的默认方案。

### 5.2 各知识类型的默认索引规则

| 知识类型 | 关系库事实记录 | 默认索引方式 |
|---|---|---|
| 文本块知识库 | `content`、分块及源位置 | 对 `content` 生成向量；携带知识库、记录、源版本、页码、分块和字符范围等元数据 |
| 问答知识库 | `question`、`answer` | 一期默认对问题生成向量、答案作为返回内容；后续可配置问答拼接或多向量策略 |
| 知识图谱三元组 | `subject`、`predicate`、`object` | 三元组写入图数据库；可选将实体或序列化三元组生成向量，用于语义召回 |
| 多轮对话知识库 | `messages` | 按对话窗口、轮次或摘要生成索引；一期默认使用带上下文的对话窗口 |

知识图谱的短期默认向量文本可以是 `subject | predicate | object`，但长期目标是图数据库与向量检索结合；三元组的规范事实仍保存在关系数据库中。

### 5.3 异步索引任务

一本书可能产生数万条知识记录，索引任务通常比关系库入库耗时更长，因此必须具备：

- 分批执行、并发控制、模型限流和实时进度
- 失败重试、检查点续跑、取消和服务重启后恢复
- 按知识记录内容指纹实现幂等写入和增量索引
- 保存批次错误、模型调用次数、Token 或费用统计
- 校验向量数量、维度、图记录数量和映射完整性
- 单条索引记录可反查知识记录、源文档版本和原始位置

模型、索引字段或维度发生变化时必须创建新的 `knowledge_index` 版本并执行重建；新版本验证通过后才能切换为“可检索”，旧版本保留以支持回滚。

建议状态：

```text
知识库：生成中 → 已入库
索引：待索引 → 索引中 → 校验中 → 可检索
                         ↘ 索引失败（可重试）
```

知识库没有可用默认索引方案时停留在“已入库，待索引”；索引失败不破坏已经入库的知识资产。

### 5.4 二期数据关系

```text
knowledge_base 1 ── N knowledge_index
knowledge_index N ── 1 index_profile
knowledge_index 1 ── N index_job
index_job 1 ── N index_batch

knowledge_record 1 ── N index_record
knowledge_index 1 ── N index_record

index_profile N ── 1 embedding_service
index_profile N ── 1 vector_store
index_profile N ── 0..1 graph_store
```

关键约束：索引记录必须引用关系库知识记录；索引版本不可原地覆盖；被应用使用的只能是校验通过的可用索引版本。

### 5.5 二期验收标准

- 技术人员可以配置并测试向量模型、向量库和图数据库连接
- 每个知识类型和结构版本可以发布一个默认索引方案
- 新知识库入库后能自动创建异步索引任务，也能手动重试或重建
- 大批量任务支持分批、限流、进度、检查点续跑和幂等写入
- 文本块、问答、三元组和多轮对话分别按已发布规则建立索引
- 索引完成后校验记录数、向量维度及关系库映射完整性
- 任一检索索引记录可以追溯到知识记录和源文档原始位置
- 更换模型、维度或索引规则时生成新版本，不覆盖正在使用的版本

## 6. 三期：知识集合与应用交付

三期不直接把多个知识库内容复制到一个大库，而是建立“同类、索引兼容知识库的版本化集合”。

建议对象：

- `knowledge_collection`：稳定的集合身份和名称
- `collection_version`：一次不可变的集合快照
- `collection_member`：快照包含的知识库编号
- `application_binding`：应用绑定的集合版本或已发布别名

集合约束：

1. 一个集合只能包含同一种知识库类型。
2. 成员必须有可用索引，并使用兼容的索引方案、模型和向量维度；不兼容时先用统一方案重建索引。
3. 已发布集合版本不可原地修改；增删成员生成新版本。
4. 对外提供 `collection_version_id` 作为可复现的调用标识。
5. 如需始终指向最新版，可额外提供发布别名，但别名变更必须审计。
6. 集合查询结果必须继续保留到知识记录和源文档的溯源链。

三期流程：

```text
多个同类知识库
  → 创建知识集合
  → 校验类型和可用索引兼容
  → 生成不可变集合版本
  → 发布集合标识
  → 应用绑定并消费
```

这层是资产交付与组合管理，不负责重新加工数据。

## 7. 四期：可配置 RAG 应用

四期在三期稳定知识接入之上组合 LLM 与 Prompt，不让应用版本直接依赖物理索引：

```text
应用稳定标识
  → 不可变应用版本
  → Application Binding + LLM + Prompt + 生成参数
  → 检索增强生成
  → 回答、引用、Token、耗时与运行审计
```

配置面包括应用创建、版本草稿、输入输出 Schema、Prompt 输入映射、真实依赖探测、预览调试和发布切换。生产调用面以稳定 `app_key` 提供同步/SSE Invoke API，支持应用级哈希密钥、当前版本路由、固定发布版本、引用返回和调用审计。检索方案现已支持可选 Reranker；后续增强限流配额、查询改写、混合检索、结构化输出、工具调用与自动评测。

### 7.1 内部部署基线

当前部署目标是单台内部服务器，不扩展 SaaS 权限与计费体系。代码、DataFlow、SQLite 状态目录和 Milvus 采用明确的目录与持久化边界；systemd 管理单个 DataForge 进程。发布前使用 `dataforge doctor --deep` 验证模型与存储，启动后使用 liveness、readiness 和 `dataforge smoke` 验证 HTTP 服务面。可选依赖未启用时允许降级，但状态目录或 SQLite 不可用时必须拒绝就绪。

## 8. 五期：更多来源和医疗能力

- Excel（XLSX 已接入）、数据库、API、HIS、EMR
- 扫描 PDF OCR
- 医疗实体识别、ICD 和药品标准化
- 数据质量评估、人工抽检、敏感信息治理

## 9. 当前验收标准

- 能上传并版本化管理 PDF、CSV、XLSX、Markdown、DOCX、TXT、JSON、JSONL
- 页面只选择知识类型，系统自动匹配兼容且已验证的默认标准流程
- 后端拒绝类型不兼容或未验证的流程
- 能对多个源文档并行执行文本块流程
- 输出逐条格式验证，通过后才生成知识库
- 知识库有稳定编号、名称、类型、来源和流程信息
- 任一知识记录可以追溯到源文档版本和源位置
- 一期的“已入库”只代表关系库知识资产已经生成，不代表已经向量化或可检索
- DataFlow 流程画布、算子、样本执行、日志和结果等必要开发能力保持可用
- 任一跨页面操作都能返回来源页面，多步骤操作可取消且不丢失来源路径
- 页面中所有可见按钮均有真实操作、状态反馈和完成后的下一步
- 算力服务可以配置、编辑、测试，密钥不在列表或日志中明文显示
- 可以把多个兼容知识库发布为不可变集合版本，并通过稳定接入标识查询
- 可以配置并发布 RAG 应用版本，真实调用 Embedding、Milvus 和 LLM
- 检索方案可以按版本启停 Reranker，并保留向量分、重排分和执行审计
- 每次应用运行可以回溯到应用版本、知识集合版本、引用记录、Token 与耗时
- 业务应用可以只依赖稳定 app_key、API Key 和版本化输入输出契约调用当前发布链路
- 可以固定调用任一历史发布版本，并通过 SSE 接收模型增量输出

## 10. 当前不做

- 登录、角色、租户和复杂审批
- 把不同类型的知识库放入同一集合
- 将尚未验证的 DataFlow 流程用于生产入库
- Agent/工具调用、自动评测、租户权限、限流与计费体系
